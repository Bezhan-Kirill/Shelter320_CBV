Платформа питомника предназначена для записи собак, включает в себя раздел Породы (Различные породы собак),
Собаки (Список записаных собак), Список пользователей а так же Рецензии. Реализована возможность
добавления/изменения/удаления
собак или отзывов

Виртуальное окружение используемое для проекта: venv

1) После настройки виртуального окружения установите зависимости из файла requirements.txt

```bash
pip install -r requirements.txt
```

2) Заполните файл .env file согласно файла .env.sample


3) Создайте Базу данных при помощи команды

```bash
python manage.py ccdb
```

4) Создайте миграции при помощи команды

```bash
python manage.py makemigrations
```

5) Примените созданные миграции

```bash
python manage.py migrate
```

6) Выполните команду для создания пользователей

```bash
python manage.py ccsu
```

7) Выполните команду для заполнения базы данных используя фикстуры

```bash
python manage.py loaddata sections.json
```

8) Выполните команду для запуска приложения

```bash
python manage.py runserver
```

Модели используемые в проекте

Dog с полями:

- name - кличка
- category - порода
- photo - фото (может быть пустым)
- birth_date - дата рождения
- is_active - признак активности собаки по умолчанию True
- owner - владелец
- views - количество просмотров

Category с полями:

- name - порода
- description - описание

Parent с полями:

- dog - имя потомка
- name - имя родителя
- category - порода
- birth_date - дата рождения

User с полями:

- username - имя пользователя
- email - электронная почта пользователя
- role - роль пользователя (USER or MODERATOR or ADMIN)
- first_name - имя пользователя
- last_name - фамилия пользователя
- phone - телефон пользователя (может быть пустым)
- telegram - телеграм (может быть пустым)
- avatar - аватар (может быть пустым)
- is_active - признак активности пользователя по умолчанию True

Review с полями:

- title - заголовок
- slug - уникальный идентификатор создается случайно
- content - содержимое
- created - дата создания
- sign_of_review - признак активности отзыва по умолчанию True
- author - автор (может быть пустым)
- dog - собака к которой отзыв

Пагинация

- Реализована пагинация для списка собак (3 элемента на страницу) для списка отзывов (2 элемента на страницу) и для
  списка пользователей (2 элемента на страницу)